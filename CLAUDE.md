# The Prophet — site
Front end of theprophet.fun. Static HTML + CSS, mobile first, handed to the CTO who drops it into Rust server templates. No React, no framework, no build step, no npm. Vanilla JS only for six things: carousel swipe/selection, menu button, first-visit walkthrough (remembered on device), typewriter placeholder, roadmap timeline light, contract-address copy button. Everything the server fills in is a clearly marked template variable, written as {{name}} until the CTO's syntax is confirmed; list every variable used in TEMPLATE_VARS.md.

## Files
site/index.html (home) · prophecies.html · record.html · prophet.html · partials/header.html, below-fold.html, footer.html · styles.css · app.js · assets/. Mock data inline: 12 sample markets, a few sample theses, one sample Record entry. Partials are canonical in site/partials/. Each page carries a copy wrapped in <!-- partial:header --> ... <!-- /partial:header --> markers so the CTO replaces them with server includes. Repeated rows (markets, takes) have one template-variable copy inside a <template> element so it never renders from disk; the server loop replaces the <template>. All asset paths are relative so pages open directly from disk; the CTO may rewrite to absolute on his server.

## Brand
Colors, max four: navy #151B26 (canvas), deep purple, glow green, off-white. Orbitron 500, all caps, letterspaced for wordmark and headings. Space Grotesk for body and UI. Logo flat with no glow. No orb or crystal ball anywhere. Flat, thick outlines, esports-badge feel. Wordmark and logo come from assets/, never typed as text.

## Layout
Header: logo left (mark ~48px mobile / 58px desktop; wordmark ~13px / 16px), links home. "How it Works" anchor (#how-it-works) right of the logo lockup, desktop only; first item in the mobile menu panel. Desktop nav: Prophecies · Record · $PROPHET · Docs (Docs links out), then at the far right a small outlined monospace pill "CA {{contract_address}}" shown as first 6 … last 6 with a copy icon; clicking copies the full address and the pill reads "copied" in green for a moment. This is the one address shown anywhere; it is the token contract, never a wallet. Mobile: logo + one menu button top right with How it Works, the four routes, Docs, the CA copy pill, and account (signed out: [COPY] label; signed in: {{username}}). No other button in the header.
Hero: fills the viewport below the header (no line at its bottom), one slot, four panels by route; content sits in a centered column (max 1200px) with side padding, 16px mobile / 48px desktop. Below-fold sections start after the fold. Record and Prophecies scroll inside the hero. Bar pinned at the hero's bottom on mobile.
Below every panel, no boxes anywhere (hairlines only): How it Works (5 numbered steps in a row, green numbers on a hairline) → Tokenomics (heading, one intro sentence kept on one line on desktop, dropping a font step rather than wrapping; the four streams as a plain table Stream · Share · What it does; then four dashed notes, the first being the launch facts; no sub-headers, no bar graphic; Buy $PROPHET) → Roadmap (one-liners start with a capital; a horizontal hairline timeline on desktop, one dot per phase with the phase's items under it, stacking to a vertical line on mobile; a long streak of green light travels the line continuously without pausing; dots sit over the line so the streak is never visible inside one, and each dot fills solid when the streak's leading edge is ~10% into it and drains when the trailing edge is ~10% past it; subtle glow, looping) (Phase 1 Rewards · Seeker App · TradeLeague Pools; Phase 2 War Chest · Proposals · The Temple; Phase 3 Summon · Your Positions · Counsel; each word with its one line under it, no dates). Below-fold copy is final and static; the only variable there is {{buy_url}}.. Footer: one short row under the Roadmap only, never visible in the hero: left "Automated by" (gray) + TradeLeague logo (assets/tradeleague-logo.svg, the one partner mark allowed outside the palette); right, three groups split by hairlines, no titles: the TradeLeague bull mark only (assets/tradeleague-mark.svg, in TradeLeague green) + X, Telegram, LinkedIn; The Prophet mark + X, Telegram, LinkedIn; the $PROPHET ticker + Jupiter, pump.fun, Solscan official logos (assets/jupiter.png, pumpfun.png, solscan.png, 16px round, always grayscale, hover brightens to white like the social icons). One row where it fits, wrapping to two otherwise.
Home: horizontal carousel of 12 cards, the pinned market first but with no pinned tag or any other marker, sitting near the top of the hero so the feed below has room; card = Polymarket market image (44px square, top left), question (two lines max), YES % large with a "yes" label beside it, one icon meta line (bar-chart icon + volume, clock icon + ends-in relative time, person icon + take count), state badge top-right only for vote open (vote open · 62% YES) or called (called YES · bolt icon + conviction N); markets with no thesis show no state at all; every listed market is open so "open" is never shown; no "as of" time on cards, thesis line if any (one line, ellipsized). Cards are a fixed height so switching filters never shifts the layout. One card fills mobile width with next card's edge peeking; three across on desktop. Three filter tags above the carousel (No thesis · Vote open · Called), one active at a time, click again to clear. Exactly one card is selected (brighter outline); clicking a card selects it; arrows on either side (desktop only) rotate the deck one card at a time as a continuous loop in either direction (never a visible jump back to the start) and select the card that lands leftmost; arrow keys (left/up = previous, right/down = next, wrapping) move the selection to the neighbouring card and the takes, thesis and typed question follow, rotating the deck only when that card is off-screen; swipe/snap selects on mobile. Takes and the bar's typed question follow the selected card. The bar is the hero of the panel: the selected market's question is typed into the empty field as placeholder with a typewriter animation (~30ms/char, blinking cursor), held ~2s, erased, then the next market's question, looping; restarts from the selected card when the selection changes; the field clears the moment the user types. Bar = text field + green send button only, just under half the viewport wide and centered on desktop, full width and pinned at hero's bottom on mobile. Desktop stacks cards near the top, then the feed fills the space down to the bar, so nothing moves when the selection changes. Above the bar, one fixed-height scrolling feed: for called or vote-open markets a Thesis block sits at the top, outlined in the same brighter gray as the selected card (label "Thesis", or "Thesis draft" while the vote is open, never the word draft in the text; the thesis stance YES/NO in green/red right after the label; state badge on the right, and for vote open a purple outlined Vote button beside it that opens a small popup with "Your vote" YES/NO, the button then reads "Voted YES" with the other option still live; the full thesis, at least two lines; then under a hairline the contributors as rows of username + one-line contribution + XP, no heading), then takes as two-line rows (left: {{take.username}} + stance tag over the claim; right column: relative timestamp over "+{{take.xp}} XP"); on desktop the list is a fixed height and scrolls, so the centered block never resizes. Empty state is a template variable so it can be generated later: {{empty_no_takes}} when the market has no takes. Nothing is shown under a non-empty list. Directly under the bar, one row: Polymarket status on the left ("● Polymarket", small green dot with a pulsing glow when up, red when down; "as of {{polymarket_as_of}}" on hover) and an empty right-aligned slot reserved for future prompt tool buttons. First-visit walkthrough: three [COPY] cards, Next/Done, remembered in localStorage; no "?" or help button anywhere.
Prophecies: open votes first (draft, live tally in weight, time left, YES/NO buttons; a voted state shows your choice with the other button still live), then live theses, then history. Buy $PROPHET.
Record: stats line (Calls · Wins · Losses · Win rate · Edge); entries newest first, expandable to the chain (call → updates → resolution → lesson). Empty state.
$PROPHET: chart embed placeholder, pump.fun link, four doors table, wallet links as labels, last week's pool line, custody line, Buy $PROPHET.
Socials live in the footer only; Docs stays in the header.
Prophecies and Record panels use outlined table rows, one layout, no view toggle.

## Style
Reference: orbmarkets.io on our palette. Canvas #151B26, flat — no grid, no pattern, no texture. A faint purple radial glow centered behind the hero at ~8% opacity, fading to nothing by the hero's edges. Every panel, row, card, and input has a 1px outline in off-white at ~15% opacity; no fills, no gradients, no shadows, no rounded corners beyond 4px. Green is the only accent: status dot, active nav, YES tallies, links, send button. The Vote button is the one purple control, matching the vote state it belongs to. Purple only in the logo, the hero glow (deep #5F3A8B), and draft/vote text (brighter #B08CF5 so it reads on navy). Vote tallies read "62% YES" with the tally colored green for YES, red for NO; never "by weight". Numbers in tabular figures. Orbitron 500 caps letterspaced for the wordmark and section headings; Space Grotesk for everything else. Dense, sleek, wireframe; it should feel like an agent's terminal, not a marketing page.

## Hard rules
Zero crypto-speak. Never show a wallet address; people have usernames. No calendar dates anywhere; times are relative. Losses shown next to wins. Where copy needs the character's voice (empty states, walkthrough cards, signed-out label), write [COPY] placeholders; never invent copy. Never add features, sections, or mechanics not in this file; if something seems missing, stop and ask. Small diffs, one step per prompt, explain each change in one line.
