# The Prophet — site
Front end of theprophet.fun. Static HTML + CSS, mobile first, handed to the CTO who drops it into Rust server templates. No React, no framework, no build step, no npm. Vanilla JS only for nine things: carousel swipe/selection, menu button, first-visit walkthrough (remembered on device), typewriter placeholder, roadmap timeline light, contract-address copy button, the $PROPHET price chart, the small tab/toggle controls (filters, ranges, Price/MCap, vote popups), and the circuit board behind the prompt bar. Everything the server fills in is a clearly marked template variable, written as {{name}} until the CTO's syntax is confirmed; list every variable used in TEMPLATE_VARS.md.

## Files
site/index.html (home) · prophet.html · partials/header.html, onboarding.html, below-fold.html, footer.html · styles.css · app.js · assets/. Mock data inline: 12 sample markets, a few sample theses, one sample Record entry. Partials are canonical in site/partials/. Each page carries a copy wrapped in <!-- partial:header --> ... <!-- /partial:header --> markers so the CTO replaces them with server includes. Repeated rows (markets, takes) have one template-variable copy inside a <template> element so it never renders from disk; the server loop replaces the <template>. All asset paths are relative so pages open directly from disk; the CTO may rewrite to absolute on his server.

## Brand
Colors, max four: navy #151B26 (canvas), deep purple, glow green, off-white. Orbitron 500, all caps, letterspaced for wordmark and headings. Space Grotesk for body and UI. Logo flat with no glow. No orb or crystal ball anywhere. Flat, thick outlines, esports-badge feel. Wordmark and logo come from assets/, never typed as text.

## Layout
Header: logo left (mark ~48px mobile / 58px desktop; wordmark ~13px / 16px), links home. "How it Works" right of the logo lockup on desktop and first in the mobile menu; it opens the onboarding modal, every time. Desktop nav: $PROPHET · Docs (Docs links out), then at the far right a small outlined monospace pill showing {{contract_address}} as first 6 … last 6 with a copy icon, no "CA" label; clicking copies the full address and the pill reads "copied" in green for a moment. This is the one address shown anywhere; it is the token contract, never a wallet. Mobile: logo mark + wordmark, the address pill (shortened to 4 … 4) and one menu button. The menu holds How it Works, $PROPHET and Docs, then the footer's social and token links at the bottom; no account row. No other button in the header.
Hero: fills the viewport below the header (no line at its bottom), one slot, two panels by route; content sits in a centered column (max 1200px) with side padding, 16px mobile / 48px desktop. Below-fold sections start after the fold. Bar pinned at the hero's bottom on mobile.
Below every panel, no boxes anywhere (hairlines only): How it Works (5 numbered steps in a row, green numbers on a hairline) → Tokenomics (heading, one intro sentence kept on one line on desktop, dropping a font step rather than wrapping; the four streams as a plain table Stream · Share · What it does; then four dashed notes, the first being the launch facts; no sub-headers, no bar graphic; Buy $PROPHET) → Roadmap (one-liners start with a capital; a horizontal hairline timeline on desktop, one square dot per phase with the phase's items under it, stacking to a vertical line on mobile; a long streak of green light travels the line continuously without pausing; dots sit over the line so the streak is never visible inside one, and each dot fills solid when the streak's leading edge is ~10% into it and drains when the trailing edge is ~10% past it; subtle glow, looping) (Phase 1 Rewards · Seeker App · TradeLeague Pools; Phase 2 Trading Wallet · Proposals · The Temple; Phase 3 Summon · Your Positions · Counsel; each word with its one line under it, no dates). Below-fold copy is final and static; the only variable there is {{buy_url}}.. Footer: one short row under the Roadmap only, reduced to the TradeLeague credit alone on mobile, never visible in the hero: left "Automated by" (gray) + TradeLeague logo (assets/tradeleague-logo.svg, the one partner mark allowed outside the palette); right, three groups split by hairlines, no titles: the TradeLeague bull mark only (assets/tradeleague-mark.svg, in TradeLeague green) + X, Telegram, LinkedIn; The Prophet mark + X, Telegram, LinkedIn; the $PROPHET ticker + Jupiter, pump.fun, Solscan official logos (assets/jupiter.png, pumpfun.png, solscan.png, 16px round, always grayscale, hover brightens to white like the social icons). One row where it fits, wrapping to two otherwise.
Home: horizontal carousel of 12 cards in this order — markets with an open vote first, then markets with a live thesis (most recently called first, at most twelve), then markets with no thesis — with no pinned tag or any other marker, sitting near the top of the hero so the feed below has room; card = Polymarket market image (44px square, top left), question (two lines max), YES % large with a "YES" label beside it (probabilities always carry a capitalized YES), one icon meta line (bar-chart icon + volume, clock icon + ends-in relative time, person icon + take count), state badge top-right only for vote open (vote open · 62% YES) or called (called YES · bolt icon + conviction N); markets with no thesis show no state at all; every listed market is open so "open" is never shown; no "as of" time on cards. The bottom row carries the state on the card face: open vote → the card is identical to every other card, plus a small outlined green Buy $PROPHET link floating over the bottom-right corner on the meta row's line; the vote itself is the joint YES | NO switch at the far left of the prompt bar (same vote as the thesis block's popup, so the two never disagree); live thesis → the meta row with "{{market.called_pct}} at call" first, the position and star conviction staying in the badge; no thesis → the meta row as is. Cards are a fixed height so switching filters never shifts the layout. One card fills mobile width with next card's edge peeking; three across on desktop. Four filter tags above the carousel (No thesis · Open votes · Prophecies · History), one active at a time with one gray highlight for all four (no purple or green tags), click again to clear; the Tab key cycles through the four in order and Shift+Tab goes back. History is its own deck: the last ten to twelve resolved markets, newest first, in the same track but shown only while that tag is on; its cards keep the structure (badge: called YES + star conviction + a WON/LOST tag; the big figure is the settled YES %, 100% or 0%; meta row as for called) and its thesis block adds one line under the reasoning, "Resolved YES · market closed at 97% · 2w ago". Exactly one card is selected (brighter outline); clicking a card selects it; arrows on either side (desktop only) rotate the deck one card at a time as a continuous loop in either direction (never a visible jump back to the start) and select the card that lands leftmost; arrow keys (left/up = previous, right/down = next, wrapping) move the selection to the neighbouring card and the takes, thesis and typed question follow, rotating the deck only when that card is off-screen; swipe/snap selects on mobile. Takes and the bar's typed question follow the selected card. The bar is the hero of the panel: the selected market's question is typed into the empty field as placeholder with a typewriter animation (~30ms/char, blinking cursor), held ~2s, erased, then the next market's question, looping; restarts from the selected card when the selection changes; the field clears the moment the user types. Bar = a joint YES | NO switch on the left, always shown (the stance for what you type; for an open vote it is also the vote, remembered per market) + text field + green send button, just under half the viewport wide and centered on desktop, full width and pinned at hero's bottom on mobile. Desktop stacks cards near the top, then the feed fills the space down to the bar, so nothing moves when the selection changes. Above the bar, one fixed-height scrolling feed: for called or vote-open markets a Thesis block sits at the top, outlined in the same brighter gray as the selected card (label "Thesis", or "Thesis draft" while the vote is open, never the word draft in the text; then in a row: the position YES/NO in green/red, the estimated probability as "78% est.", the star conviction; state badge on the right, and for vote open a purple outlined Vote button beside it that opens a small popup with "Your vote" YES/NO, the button then reads "Voted YES" with the other option still live; the full thesis, at least two lines; then under a hairline the contributors as rows of username + one-line contribution + XP, no heading; nothing else — no sources, no references), then takes as two-line rows (left: {{take.username}} + stance tag over the claim; right column: relative timestamp over "+{{take.xp}} XP"); on desktop the list is a fixed height and scrolls, so the centered block never resizes. Empty state is a template variable so it can be generated later: {{empty_no_takes}} when the market has no takes. Nothing is shown under a non-empty list. Directly under the bar, one row: Polymarket status on the left ("● Polymarket", small green dot with a pulsing glow when up, red when down; "as of {{polymarket_as_of}}" on hover) and an empty right-aligned slot reserved for future prompt tool buttons. Onboarding modal (partials/onboarding.html, on both pages): opens on a first visit and is remembered on the device so it never shows twice; the How it Works link opens it every time; closes on the X, Escape, or the backdrop; body scroll locked behind it. Three slides, each a looping video slot on top (16:9; placeholder frames in assets/onboarding-N-placeholder.png until the real muted autoplay loop playsinline recordings are dropped in), a title, one line, square dots, Next (Get Started on the last, which closes it) and Back on slides 2 and 3; arrow keys and swipe move between slides. Copy is final and lives in the partial. Mobile: full width, near full height, video smaller, buttons 44px. No "?" or help button anywhere.
Prophecies and Record pages: archived, not deleted — `archive/prophecies.html` and `archive/record.html`, out of the nav and the build. Everything they showed lives on the home carousel. The full site with both pages is also on the `v2` branch and the `v2-record-page` tag.
$PROPHET: inside the scrolling hero, top to bottom: a six-cell stat strip, two lines per cell, label over value with the 24h change inline beside it (Price · Market cap · Vol 24h · Liquidity · Holders · Fees earned in green); stat numbers use the body face, not the display face, so they read; then our own chart on the left and our own swap panel on the right (no third-party badge, no Buy button on this panel): the chart carries the live price and change, a Price/MCap segmented control, 5M · 1H · 6H · 1D · 1W range tabs, a filled green area line with a dashed guide at the last price, an x-axis labelled with clock times on short ranges and dates on long ones (the chart is the one place absolute times appear), and the live price tagged on the y-axis beside the green dot; the swap is pay/receive fields with a flip button, a meta line with slip and impact on the left and the viewer's SOL balance on the right, and a Buy $PROPHET action. Then a live-trades strip, one pill per trade with a green BUY or red SELL tag, scrolling sideways. Then Wallets: four outlined rows named by stream, each a link out labelled "Public wallet" with a live balance in green, except Buy-and-Burn which shows the burned amount in red with the $PROPHET ticker in gray and a red flame icon; never an address. Then "Last week's pool" with the amount, paid to the community. No streams table and no custody line here; the streams table lives in Tokenomics below the fold. The whole panel is sized to fit one hero without scrolling on a laptop. On mobile the order changes to chart, live trades, swap, then wallets, and the price, Price/MCap control and range tabs share one row.
Socials live in the footer only; Docs stays in the header.

## Style
Reference: orbmarkets.io on our palette. Canvas #151B26, flat — no grid, no pattern, no texture. Behind the prompt bar only, inside the hero, desktop (1024px+): a circuit board. Traces leave the bar's frame at 90 degrees, never off a corner, run straight, turn at right angles and end in a small square pad. Two to four legs, no diagonals, no curves, nothing joins into a web, nothing is drawn over text or a card, and nothing rises above the top of the cards. Invisible at rest. Every 3 seconds the bar's frame swells and exactly one trace lights: a 160px green streak in the roadmap's language, peaking at 0.5 against its 0.9, with the pad filling as the light lands and draining behind it. A fresh route is generated for every flash by a self-contained route planner, which owns shape and length: a route may only move in one horizontal and one vertical direction so it can never double back, its length is drawn from weighted classes so runs are not all long, and it takes one of five shapes. Runs go far, up to 950px, at the roadmap's own 200px/s, with climbs that zigzag up the channel between the carousel's arrow and its cards, never passing the arrow, and never the same side of the bar three times running; a long one is still travelling when the next flash fires and that is fine, so at most two are on screen. What never varies is the beat: exactly one trace leaves the bar per flash. Traces are bounded by the hero on all four sides, so nothing runs off the page. Console: window.prophetBoard.set({beat, speed}). Nothing renders below 1024px or on any page without a prompt bar. Every panel, row, card, and input has a 1px outline in off-white at ~15% opacity; no fills, no gradients, no shadows, no rounded corners beyond 4px. Green is the only accent: status dot, active nav, YES tallies, links, send button. The Vote button is the one purple control, matching the vote state it belongs to. Purple only in the logo, the hero glow (deep #5F3A8B), and draft/vote text (brighter #B08CF5 so it reads on navy). Vote tallies read "62% YES" with the tally colored green for YES, red for NO; never "by weight". Numbers in tabular figures. Orbitron 500 caps letterspaced for the wordmark and section headings; Space Grotesk for everything else. Dense, sleek, wireframe; it should feel like an agent's terminal, not a marketing page.

## Hard rules
Zero crypto-speak. Never show a wallet address; people have usernames. No calendar dates anywhere; times are relative. Losses shown next to wins. Where copy needs the character's voice (empty states, walkthrough cards, signed-out label), write [COPY] placeholders; never invent copy. Never add features, sections, or mechanics not in this file; if something seems missing, stop and ask. Small diffs, one step per prompt, explain each change in one line.
